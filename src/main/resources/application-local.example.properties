# Copy to application-local.properties and fill with QA-only values.
# Do not store production credentials in this repository.

cx.printing.template-path=src/main/resources/reports
cx.printing.logo-path=src/main/resources/static/logo
cx.printing.output-path=target/boxlabel-output

# Enable with: --spring.profiles.active=sqlserver,local
# The same connection is currently used for:
# - Java-owned LP_* tables
# - read-only legacy label sources such as hsh_Name_Conversion, Inventory, CD_PRINT_TEMPLATE
# Later, split these into separate data sources if deployment requires physical isolation.
# cx.printing.sql-server-url=jdbc:sqlserver://HOST:1433;databaseName=DB;encrypt=false
# cx.printing.sql-server-username=readonly_user
# cx.printing.sql-server-password=change-me
